package lesson8;

import javax.sound.sampled.*;

public class Cat extends Animal {
    public Cat (String name, String age, String kind, String voice)
    {
    super (name, age, kind, voice);
    }
}
